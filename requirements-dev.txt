# Verification-only dependencies. Deliberately not in requirements.txt so the
# published container carries nothing it does not run.
-r requirements.txt
pytest==9.0.1
pytest-asyncio==1.3.0
httpx==0.29.2
